# ----------------------------------------------------------------------------
# Qwen-MCP Server Environment Configuration
# ----------------------------------------------------------------------------

# 1. Cloud Provider (Alibaba DashScope)
# Get your API key from: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 2. Local Privacy-First Provider (Ollama)
# Uncomment to run Models 100% locally. Overrides DASHSCOPE_API_KEY.
# OLLAMA_BASE_URL=http://localhost:11434/v1

# 3. Model Configuration (Optional Overrides)
# If using Ollama, set this to your pulled model (e.g., qwen2.5-coder:latest)
# QWEN_MODEL_NAME=qwen-plus

# 4. The Lachman Protocol Circuit Breaker
# Maximum number of times the architect will retry self-healing before halting.
LP_MAX_RETRIES=3

# 5. Security & Isolation
# Protect against LLM Path Traversal. Forces tools to stay within this folder.
WORKSPACE_ROOT=.
# Enable generic redaction of secrets from prompts before sending to cloud
SECURITY_REDACTION_ENABLED=true

# 6. Networking
# Maximum time (seconds) to wait for the LLM API to respond per request
DASHSCOPE_TIMEOUT=60.0
